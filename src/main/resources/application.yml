spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: admin
    url: jdbc:postgresql://localhost:5432/ewallet-system
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
#      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate:
        default_schema: wallet
        format_sql: true
    show-sql: true
